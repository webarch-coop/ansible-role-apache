---
- name: "Apache {{ mod }} module checked"
  stat:
    path: "/etc/apache2/mods-enabled/{{ mod }}.conf"
  register: apache_mod_status
  tags:
    - apache

- name: "Apache {{ mod }} module disabled"
  apache2_module:
    name: "{{ mod }}"
    state: absent
  when: apache_mod_status.stat.exists
  tags:
    - apache
...
